class Drivers{
      constructor(Name, Gender ){
        this.name = Name
        this.Gender = Rate
      }
    }

    const Driver1 = new Driver1("Maleunda", "Male");
    const Driver = new Driver2 ("Jeannine", "Female")
    console.log(driver1);
    console.log(driver2);

    // to create many similar but different objects we need to use the key word "class" //